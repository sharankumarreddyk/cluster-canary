apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-canary.fullname" . }}
  labels:
    {{- include "cluster-canary.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "cluster-canary.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
