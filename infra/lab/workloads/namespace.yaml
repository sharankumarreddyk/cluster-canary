apiVersion: v1
kind: Namespace
metadata:
  name: workloads
  labels:
    # chaos-daemon nsenters into target pods — privileged PSA is required.
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/warn: privileged
